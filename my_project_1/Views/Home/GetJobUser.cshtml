@model IEnumerable<my_project_1.Models.Applyforjob>

@{
    ViewBag.Title = "GetJobUser";
    Layout = "~/Views/_main_LayoutPage1.cshtml";
}

<h2>جميع الوظائف المتقدم اليها</h2>


<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.job.Jobtital)
        </th>
        
        <th>
            @Html.DisplayNameFor(model => model.Massage)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ApplyDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.job.Jobtital)
        </td>
        
        <td>
            @Html.DisplayFor(modelItem => item.Massage)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ApplyDate)
        </td>
        <td>
            @Html.ActionLink("تعديل", "Edit", new { id=item.Id }) |
            @Html.ActionLink("تفاصيل", "DetailsForJob", new { id=item.Id }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
